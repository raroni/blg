<div class="posts">
	<% if @posts.any? %>
		<% for post in @posts %>
			<div class="post">
				<h2><%= link_to_post post %></h2>
				<div class="content">
					<%= post.content %>
				</div>
			</div>
		<% end %>
	<% else %>
		<p>No posts found.</p>
	<% end %>
</div>
